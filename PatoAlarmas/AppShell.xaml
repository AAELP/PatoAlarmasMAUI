<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PatoAlarmas.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PatoAlarmas.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="PatoAlarmas">

    <!-- Definición de rutas -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />
    <ShellContent
        Title="Admin"
        ContentTemplate="{DataTemplate local:AdminPage}"
        Route="AdminPage" />
    <ShellContent
        Title="User"
        ContentTemplate="{DataTemplate local:UserPage}"
        Route="UserPage" />
    <ShellContent
        Title="Citas"
        ContentTemplate="{DataTemplate local:CitasPage}"
        Route="CitasPage" />
    <ShellContent
        Title="Vehiculos"
        ContentTemplate="{DataTemplate local:VehiculosPage}"
        Route="VehiculosPage" />

</Shell>
